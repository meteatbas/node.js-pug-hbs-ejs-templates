<%- include ('includes/head.ejs') %> 
    <h1>Users</h1>
        <% if(users.length>0) { %> 
            <ul>
                <% users.forEach((horse)=>{ %> 
                <li><%= horse.name %></li>
                <% }) %> 
            </ul>
        <% }else{ %> 
            <h1>No users found</h1>
        <% } %> 
<%- include('includes/end.ejs') %> 